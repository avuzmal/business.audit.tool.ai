@AGENTS.md

# Business Audit Tool AI - Developer Guide

- Use standard ES6 modules.
- Ensure all React components use Tailwind CSS.
- Run `npm run lint` and `npm run build` before pushing.
- All new features should have corresponding n8n webhook handlers.
